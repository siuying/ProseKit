# Prose Example

Open `ProseExample.xcodeproj`, select an iOS simulator, and run the
`ProseExample` scheme. The app hosts `ProseView` with a hardcoded
`doc(heading("Hello"), paragraph("world"))` document decoded from JSON.
